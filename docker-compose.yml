services:
  app:
    image: ${IMAGE}
    container_name: provisioning-and-flashing-tracker
    env_file:
    - ./.env
    ports:
      - 3000:3000
    networks:
      - app-network   

networks:
  app-network:
    driver: bridge